# Frontend

This directory will contain the web UI for the resume classifier, for example:

- Public landing page.
- Resume upload form.
- Results view (predicted role + confidence).
- Admin login (if handled client-side).

You can scaffold this with any framework you prefer (e.g. React, Next.js, plain HTML/JS).***
