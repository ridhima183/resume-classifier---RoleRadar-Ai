# Scripts

Place helper scripts here, for example:

- Data import / migration scripts.
- Cron-like batch jobs (e.g., nightly re-training, evaluation).
- One-off utilities for cleaning or validating datasets.

